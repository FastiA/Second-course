#include <stdio.h>
#include "tool.h"


int main(int argc, const char* argv[])
{
	int base;
	char str[10];
	int c;
	
	printf("Wwedit chisla dla funk sumi, chislo C i shisnat chislo:\n");
	scanf_s("%i %i\n", &base, &c);
	gets(str);
	printf("Widpowid:%i", end(int result, int sum, int c));
	


}

int end(int result, int sum, int c)
{
	int suma(int base, int finish);
	int system(char str[10]);

	return(result *(sum - c));
}